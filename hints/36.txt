This is a "leaf function" - it doesn't call other functions,
so you don't need a stack frame!

Steps:
1. Multiply rdi by rsi, store in rax
2. Multiply rax by rdx
3. Return (rax already has the result)

Remember: imulq can do rax = src * rax, or dst = src * dst
